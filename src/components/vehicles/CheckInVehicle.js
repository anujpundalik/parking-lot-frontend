import React from 'react';
//import { Field, reduxForm } from 'redux-form';

const CheckInVehicle = () =>{

    return (
        <div> Check In Vehicle</div>
    );
};

export default CheckInVehicle; 